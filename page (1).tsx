import Link from "next/link"; import {notFound} from "next/navigation"; import {prisma} from "@/lib/prisma";
export default async function BuildPage({params}:{params:Promise<{id:string}>}){const {id}=await params; if(!process.env.DATABASE_URL)notFound();const b=await prisma.build.findUnique({where:{id},include:{owner:true,parts:true,tasks:true,posts:{orderBy:{createdAt:'desc'}},comments:{include:{author:true},orderBy:{createdAt:'desc'}}}});if(!b)notFound();const done=b.tasks.filter(t=>t.completed).length;return <main><div className="container"><div className="hero"><div className="eyebrow">{b.status} · {b.owner.name}</div><h1>{b.year} {b.make} {b.model}</h1><p>{b.description}</p><div className="statrow"><span>Progress</span><strong>{b.progress}%</strong></div><div className="progress"><div style={{width:`${b.progress}%`}}/></div></div><div className="grid2"><div><section className="section"><div className="sectionhead"><h2>Parts</h2><span className="muted small">{b.parts.length} tracked</span></div><div className="parts">{b.parts.map(p=><div className="part" key={p.id}><strong>{p.name}</strong><div className="muted small">{p.brand ?? 'Custom'} · {p.category}</div>{p.partNumber&&<div className="muted small">PN: {p.partNumber}</div>}</div>)}</div></section><section className="section"><div className="sectionhead"><h2>Tasks</h2><span className="muted small">{done}/{b.tasks.length} complete</span></div><div className="list">{b.tasks.map(t=><div className="listitem" key={t.id}>{t.completed?'✓':'○'} {t.title}<span className="muted small"> · {t.category}</span></div>)}</div></section></div><aside><div className="card"><h3>Build owner</h3><p className="muted">@{b.owner.username}</p><Link className="btn" href="/community">Follow build</Link></div><div className="card section"><h3>Journal</h3>{b.posts.map(p=><div className="listitem" key={p.id}><strong>{p.title}</strong><p className="muted small">{p.body}</p></div>)}</div></aside></div></div></main>}
