"use client";
import {useState} from "react"; import {useRouter} from "next/navigation";
export default function NewBuild(){const r=useRouter();const [form,setForm]=useState({title:"",year:"",make:"",model:"",trim:"",description:""});const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);const res=await fetch('/api/builds',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const data=await res.json();if(data.id)r.push(`/builds/${data.id}`);else setBusy(false)}return <main><div className="container"><div className="hero"><div className="eyebrow">Garage</div><h1>Create a build</h1><p>Start with the basics. You can add parts, tasks and updates from the build page.</p></div><form className="form" onSubmit={submit}>{Object.entries(form).map(([k,v])=><div key={k}><label className="label">{k[0].toUpperCase()+k.slice(1)}</label><input className="input" required={['title','year','make','model'].includes(k)} value={v} onChange={e=>setForm({...form,[k]:e.target.value})}/></div>)}<button className="btn primary" disabled={busy}>{busy?'Creating…':'Create build'}</button></form></div></main>}
