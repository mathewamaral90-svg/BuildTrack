import { NextResponse } from "next/server"; import { prisma } from "@/lib/prisma";
export async function POST(req:Request){try{const body=await req.json();const user=await prisma.user.findFirst();if(!user)return NextResponse.json({error:'No user found. Run the seed first.'},{status:400});const build=await prisma.build.create({data:{title:body.title,year:Number(body.year),make:body.make,model:body.model,trim:body.trim||null,description:body.description||null,ownerId:user.id}});return NextResponse.json(build)}catch(e){return NextResponse.json({error:'Could not create build'},{status:500})}}
