services:
  postgres:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: buildtrack
    ports:
      - "5432:5432"
    volumes:
      - buildtrack_pg:/var/lib/postgresql/data
volumes:
  buildtrack_pg:
