import Link from "next/link"; import { prisma } from "@/lib/prisma"; import { BuildCard } from "@/components/BuildCard";
export default async function Garage(){const builds=process.env.DATABASE_URL?await prisma.build.findMany({include:{parts:true,tasks:true},orderBy:{updatedAt:"desc"}}):[];return <main><div className="container"><div className="sectionhead"><div><div className="eyebrow">Your garage</div><h1>My Builds</h1></div><Link className="btn primary" href="/garage/new">+ New build</Link></div><div className="grid">{builds.map(b=><BuildCard key={b.id} build={b}/>)}{!builds.length&&<div className="card"><h3>Your first build</h3><p className="muted">Create a vehicle project and start tracking every part and task.</p><Link className="btn primary" href="/garage/new">Create build</Link></div>}</div></div></main>}
